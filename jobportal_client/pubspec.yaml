name: jobportal_client
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  shared_preferences: ^2.0.15  # Or latest version
  image_picker: ^0.8.6
  file_picker: ^5.3.3  # For file picking
  universal_html: ^2.2.1  # For web file handling
  intl: ^0.18.1
  url_launcher: ^6.1.11

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo-dark.png
